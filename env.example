# Development (default local SQLite)
DATABASE_URL=sqlite:///./petstore.db

# Production example (PostgreSQL)
# You can also paste provider URLs that start with postgres://
# the app converts them automatically to postgresql+psycopg://
# DATABASE_URL=postgresql+psycopg://petstore_user:strong_password@db-host:5432/petstore
